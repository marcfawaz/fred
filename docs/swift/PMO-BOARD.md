# PMO Board

> **Frozen 2026-07-16 — superseded, unused.** No PMO stakeholder consumes this
> board in practice. GitHub Milestones (`swift-golive`, `swift ga`) are the
> tracked-work view now — see [`STATUS.md`](STATUS.md). Kept as historical
> record only; do not add to it.

Compact PMO-facing view of tracked work. Source of truth remains:

- `docs/swift/backlog/`
- `docs/swift/rfc/`
- `docs/swift/STATUS.md`
- `docs/swift/data/id-legend.yaml`
- `docs/swift/data/sprint.yaml`
- `docs/swift/tracks/`

Do not add new scope or statuses here that are absent from the source docs.

**Maintenance rules**

- Update this file in the same change whenever a tracked item's PMO-visible
  fields change in any source doc.
- PMO-visible fields are: owner, status, blocker, backlog ref, RFC/decision ref,
  and execution ref.
- Common trigger files: `docs/swift/backlog/`, `docs/swift/rfc/`,
  `docs/swift/STATUS.md`, `docs/swift/data/id-legend.yaml`,
  `docs/swift/data/sprint.yaml`, `docs/swift/tracks/`.
- Keep one row per active, blocked, next-up, or RFC-first tracked item.
- `Execution` column priority: GitHub issue -> PR -> working branch -> `TBD`.
- When an execution ref is known, mirror it under the backlog item as `Execution: ...`.

Last updated: 2026-07-15

## Active And Next Up

AUTHZ-07 scope decision (2026-07-15, corrected same day): the concrete,
live-testable deliverable for this candidate is one fresh installation on
**GKE**, plus a portable chart contract and a **documented** (not
implemented) contract for a future AKS/Flux workshop with Benjamin. No AKS
overlay exists in `fred` or `fred-deployment-factory` and none is created by
this task. No Swift-to-Swift upgrade compatibility is required; the sole
migration path is KEA export → fresh Swift bootstrap → canonical platform
import.

| Ticket | Sprint / workstream | Current owner | PMO status | Backlog | RFC / decision | Execution |
| ------ | ------------------- | ------------- | ---------- | ------- | -------------- | --------- |
| `AUTHZ-07` | `ROOT-BOOTSTRAP-AND-DECLARATIVE-PROVISIONING` | Dimitri | **Candidate hardening in progress — Steps 1-5 done offline, Step 6 (remaining UX/durability hardening) next.** Step 1 (live observation) done 2026-07-14: the real demo bundle import produced 3 teams, 4 team_admin grants, **10 team roles ignored**, terminal state `succeeded` anyway; root `make validation-report` came back 0 passed / 227 error, all cascading from that same missing-relation gap. Step 2 fix (same day): new private `_grant_team_role_via_import` primitive writes non-admin team-scoped grants directly (mirrors the existing `_grant_platform_role`, no `schema.fga` change, ordinary team APIs unchanged and still team-admin-gated — regression-tested); reconciliation is now fail-closed (`BundleProvisioningError`) instead of warn-then-succeed. **Step 2 live-validated 2026-07-14**: re-imported the same bundle onto the live stack, re-ran root `make validation-report` — **225 passed, 0 failed, 0 errors, 2 skipped (expected)**. **Step 3 done 2026-07-14**: the import task now starts with a canonical, durable `TaskTarget` (never a raw UUID) and its terminal event carries the full structured `MigrationResult` via a new `MigrationDetail.result`/`TaskSummary.detail` (typed, backward compatible, no new `TaskState`) — a partial reconciliation (warnings) is no longer indistinguishable from full success; `TaskActivity` shows an explicit "with warnings" flag, a failed task's error message (a generic fix, all kinds), and a disclosure with counters/warnings. Backend fred-core 260 / control-plane-backend 336 offline tests, frontend 353 vitest, `tsc`/`prettier`/code-quality all clean; OpenAPI client regenerated. **Step 4 done, live-validated 2026-07-15**: `fred-deployment-factory` reduced to pure identity/authz infrastructure — commit `05dda9e` (`refactor(AUTHZ-07): make deployment factory infrastructure-only`) plus correction `65090fd` (`fix(AUTHZ-07): align factory preflight with identity-only realm`); tracked realm templates now carry zero users/groups, legacy `app:admin`/`app:editor`/`app:viewer` roles removed (`app:service_agent` kept for M2M), k3d OpenFGA seed and the direct `fredlab-authz-migrate-swift.py` script deleted, factory-owned validation targets removed in favor of `fred/validation`, preflight now requires `service_agent` and rejects `admin`/`editor`/`viewer`. Offline: `check-pure-infrastructure`, `helm lint`/`template`, `docker compose config`, `bash -n`, OpenFGA sync all green. Live 2026-07-15: full from-scratch restart — Keycloak self-registration, bootstrap secret submission, first `platform_admin`, entry into Fred, demo-provisioning-bundle import — all succeeded, confirming empty factory → registration → Fred bootstrap → Fred import as the one tested path. UX gap observed during this run (no fix yet, tracked under Step 6): the Teams page open in the browser did not pick up newly imported teams without a manual refresh (stale RTK Query cache, import completion doesn't invalidate the team-list query). **Step 5 done offline, 2026-07-15** (corrects `91e10bb4`'s AKS-inclusive framing): portable bootstrap-secret contract (`bootstrap_token_env_var` + `secretKeyRef`, `deploy/charts/fred`) wired to the GKE/ArgoCD reference deployment in `fred-deployment-factory`; the AKS/Flux contract is documented in `deploy/README.md`, not implemented — no overlay created. Same pass closed part of Step 6: the config-seeded `platform_admin_subjects`/`platform_observer_subjects` path (`libs/fred-core`) removed outright, no replacement, root bootstrap and declarative import unaffected. Next: the rest of Step 6 (durable/recoverable import execution, bootstrap-page UX gaps) — out of this task's scope. Live GKE fresh-install validation is Dimitri's post-merge acceptance step, not a prerequisite to the offline merge verdict. **v2.1.1 consolidated follow-up review, 2026-07-15:** no high finding; root-bootstrap backend accepted. Step 6 is now the single workplan for two chart release gates (bootstrap secret leaking into the migration Job; required Control Plane migration disabled by default), durable/recoverable import execution, exact-vs-additive provisioning semantics, OpenAPI/vocabulary cleanup, proven dead-code removal, bootstrap UI accessibility/responsiveness/tests, terminal team-list refresh, failed-attempt audit/rate-limit and break-glass decisions, and GKE/AKS/S3NS rollout clarification. The two root `NOTES-*` files are retired into the canonical AUTHZ-07 and OPS-04 backlogs. **Chart secret boundary release gate closed, 2026-07-15:** `hook-migration.yaml` no longer copies `.app.extraEnvVars` into the migration Job (Deployment-only now); CI proves the token reaches the Deployment, never the Job, with the migration enabled. The other Step 6 chart release gate (migration disabled by default) remains open. | [AUTHZ-MIGRATION-BACKLOG §AUTHZ-07 candidate hardening workplan](backlog/AUTHZ-MIGRATION-BACKLOG.md#authz-07-candidate-hardening-workplan--recorded-2026-07-14) | [FRED-AUTHORIZATION-TARGET-MODEL-RFC §40-42](rfc/FRED-AUTHORIZATION-TARGET-MODEL-RFC.md) · [PLATFORM-IMPORT-RFC §10-11](rfc/PLATFORM-IMPORT-RFC.md) | branch `1986-authz-07-root-platform-admin-bootstrap-and-declarative-platform-provisioning`, commit `feat(AUTHZ-07): finalize portable fresh-install bootstrap` (`fred`) / `feat(AUTHZ-07): wire GKE reference bootstrap secret` (`fred-deployment-factory`), branch `1989-follow-up-review-and-consolidation-of-candidate-release` |
| `QUALITY-02` | `QUALITY-02` | Florian | **In progress - deadline 2026-06-06** | [BACKLOG §Phase QUALITY](backlog/BACKLOG.md) | - | `TBD` |
| `QUALITY-04` | `KF-DEAD-CODE` | Dimitri | ✅ **Done 2026-07-07** — removed the unused UMAP 3D-embedding `features/model` feature (no UI consumer, only generated client code). Dropped `umap-learn` + transitive `numba`/`llvmlite`/`pynndescent` (security-alert source); regenerated KF OpenAPI + frontend client. Mechanical dead-code deletion, RFC-exempt. | [BACKLOG §QUALITY-04](backlog/BACKLOG.md) | - (RFC-exempt) | branch `fixes` |
| `AUTHZ-01` | `RBAC-TO-REBAC` | Simon | In progress - full RBAC→ReBAC migration. RFC confirmed; new `AUTHZ` domain code registered. Additive org-level OpenFGA permissions (fine-grained); remove all `@authorize`/`authorize_or_raise`/`require_admin` (≈20 files); ownership checks kept. Phase: schema + enums (AUTHZ-02). | [AUTHZ-MIGRATION-BACKLOG](backlog/AUTHZ-MIGRATION-BACKLOG.md) | [RBAC-TO-REBAC-MIGRATION-RFC](rfc/RBAC-TO-REBAC-MIGRATION-RFC.md) | GitHub issue #1875 |
| `AUTHZ-05` | `FRED-AUTHORIZATION-TARGET-MODEL` | Dimitri | **Implemented in the v2.1.1 candidate; consolidated hardening remains tracked by AUTHZ-07 Step 6.** Target model: Keycloak authenticates only (SSO/OIDC/stable `sub`); Fred/OpenFGA owns all platform/team/resource/evaluation authorization. Platform roles never imply team data visibility. **First/second pass (2026-07-09):** closed the confirmed-live `team.owner = [user] or admin from organization` escalation (a `platform_admin`-only exception was tried and **reverted the same day** — RFC §24.7, same escalation through a different door); renamed team roles `owner`→`team_admin`, `manager`→`team_editor`, `+team_analyst`; fixed the org-scoped `CAN_READ_CONTENT`/`CAN_PROCESS_CONTENT` gap everywhere team-scoping is mechanically meaningful (RFC §27); added the first `POST /teams` bootstrap endpoint (RFC §28). **Post-review pass (2026-07-09/10, triggered by human review of PR #1957):** cut `can_create_team` to `platform_admin`-only (no other caller depended on the legacy bridge); **removed the legacy Keycloak `admin`/`editor`/`viewer` organization roles from `schema.fga` entirely** — Keycloak is now identity-only, no role bridge of any kind survives, admin-tier capabilities are `platform_admin`-only, the "any connected user" tier is deleted outright with no replacement; **decoupled teams from Keycloak completely** (RFC Part 6 §29-32) — a team is now a `team_metadata` row + OpenFGA relations only, `POST /teams` no longer creates a Keycloak group (generates `uuid4().hex` instead), zero Keycloak calls remain in team CRUD/membership, plus 3 new platform-admin-only team-registry-governance capabilities (`can_list_all_teams`, `can_delete_team`, `can_rescue_team_admin` — the last one guarded to only ever act on a team with zero `team_admin`, never a standing grant, the property that keeps it structurally different from the reverted `§24.7` escalation). **Remaining (deployment-only), superseded — see the AUTHZ-07 row below:** the config-seeded `platform_admin_subjects`/`platform_observer_subjects` path this line used to point at no longer exists in any form (fields and startup bootstrap logic removed outright, AUTHZ-07); there is no configuration-population task left to do. The first `platform_admin` comes from root bootstrap, every other platform/team role from the declarative import. The RFC §24.8 readiness-report endpoint is a follow-up, not a launch blocker. Verified: fred-core 266 pass/1 skipped, control-plane-backend 259 pass, knowledge-flow-backend 332 pass, fred-runtime 415 pass; `make code-quality` clean on all four; frontend `tsc`/prettier clean; Alembic migration upgrade/downgrade verified. **Live-campaign findings (2026-07-11, items 10-11, found while standing up the `fred-deployment-factory` validation campaign):** fixed a `promote_prompt` cross-team write gap (target team was never permission-checked, item 10); found and replaced a second, dead frontend permission system that had silently disabled 6 routes and 3 controls for everyone including `platform_admin` since Keycloak app roles were removed (item 11) — new pattern documented in `docs/swift/platform/FRONTEND-AUTHZ-PATTERN.md`, `PermissionSummary` contract shrunk (`CONTROL-PLANE-PRODUCT-CONTRACT.md` §14); added a live in-browser "authorization self-test" (`/admin/self-test`) that can now probe any account via Keycloak direct-grant. Live validation suite now at 215 passing assertions (3 new scenario files in `validation/scenarios/`, now living in the `fred` monorepo itself). **Item 8b closed (2026-07-13):** removed `groups_list_to_relations`/`_user_contextual_relations` (the Keycloak JWT `groups`-claim-derived `team_member` fallback) from `RebacEngine` entirely, plus the direct call from `teams/service.py::_get_team_permissions_for_user` and the now-unused `keycloak_client`/`m2m_security` engine dependency. Closed on an explicit waiver from Dimitri (no confirmed production-data migration — `fred-deployment-factory` is being rebuilt clean, migration tracked separately there), not on the prior verbal go-ahead. Every team membership is now exclusively a persisted OpenFGA tuple. Verified offline: fred-core 255 passed, control-plane-backend 277 passed, `make code-quality` clean on both; 2 new OpenFGA integration tests written but not run live this session. **Final mechanical sweep (2026-07-13):** removed every remaining Keycloak-groups producer/consumer — `KeycloakUser.groups`, the JWT `groups` read in `oidc.py`, `KPIActor.groups` + its OpenSearch mapping, and `RuntimeContext.user_groups` (a confirmed dead field, zero real producer) plus its `MetricsProvider`/adapter plumbing; regenerated `fred-runtime` OpenAPI + the frontend runtime API client for that contract field removal (`RUNTIME-EXECUTION-CONTRACT.md` §8.13); fixed `KEYCLOAK.md`/`REBAC.md`/`FEATURES.html`/`COPYLEFT-DEPENDENCIES.md` and several stale "Keycloak group membership" comments/docstrings that no longer matched the code. Verified offline: fred-core 256, fred-sdk 186, fred-runtime 417, knowledge-flow-backend 356, control-plane-backend 277 all passed; `make code-quality` clean on all five; frontend `tsc --noEmit` clean. No live tests run. **`AUTHZ-07` / RFC Part 8 — revised 2026-07-14 (§42) after a review pass found two gaps before merge, on branch `1986-authz-07-...`:** the deployment-only "populate `platform_admin_subjects`" item above is superseded — no identity ever declared in deployment config. Review found: (1) an attacker holding only the deploy secret could name any Keycloak user as `identifier` and grant them `platform_admin` without consent; (2) the live "zero `platform_admin`" check was the same bug shape as the reverted `§24.7` escalation. Corrected: JWT now required in addition to the secret (self-promotion only, no `identifier`), and the completed-once guard is now a durably persisted marker (`PlatformBootstrapStore`, new table + migration) instead of a live OpenFGA count — surviving even a later total loss of `platform_admin` (recovery from that is an explicit separate non-goal, a future break-glass procedure). Secret never generated or logged by Fred; supplied via `bootstrap_token_env_var` (Kubernetes Secret) or `bootstrap_token_file` (local dev, `make bootstrap-token`). fred-core 256 + control-plane-backend 290 passed offline, `make code-quality` clean on both. Not yet templated for GKE/GCP or AKS. Declarative platform-provisioning operation for teams/roles/users (likely a hardened, generalized `PLATFORM-IMPORT-RFC.md`) remains open. **AUTHZ-07 hardening pass, same day (2026-07-14), before first local-docker test — 9 findings fixed, developer sign-off per item:** empty/trivial secret no longer accepted (`min_length=16` + configured-secret floor); `make bootstrap-token` rewritten atomic + fail-closed (was fail-open on `openssl` failure); write order swapped (OpenFGA tuple before the durable marker — the old order could permanently brick bootstrap on any transient OpenFGA failure, no recovery path; safe now via existing `on_duplicate_writes=IGNORE` idempotency); env-var-configured-but-absent no longer falls back to the file; new fail-closed guards for ReBAC disabled and for Keycloak/OIDC disabled (the latter found while testing the former — a mocked identity would otherwise silently degrade "two independent proofs" to one); `PlatformBootstrapStore` moved out of `fred-core` (zero consumers outside control-plane-backend) into `control-plane-backend` itself, zero Alembic drift verified; closed 3 test-coverage gaps (real-SQLite store test, route-level 401/503/200 tests, a genuine concurrency test proving the advisory lock serializes racing callers); OpenAPI client regenerated with explicit 403/409/503 responses declared. Verified offline: control-plane-backend 304 passed (full suite), fred-core 256 passed offline, frontend `tsc --noEmit` clean; `make code-quality` not run this session, outstanding before merge. | [AUTHZ-MIGRATION-BACKLOG §AUTHZ-05](backlog/AUTHZ-MIGRATION-BACKLOG.md) | [FRED-AUTHORIZATION-TARGET-MODEL-RFC §40-42](rfc/FRED-AUTHORIZATION-TARGET-MODEL-RFC.md) | [PR #1957](https://github.com/ThalesGroup/fred/pull/1957) (closes issue #1912); AUTHZ-07 on branch `1986-authz-07-root-platform-admin-bootstrap-and-declarative-platform-provisioning` |
| `RUNTIME-07` | `RUNTIME-AUTHZ-HARDENING` | Dimitri | ✅ **IMPLEMENTED 2026-06-28 on branch 1853 — awaiting review + local-Docker validation (not merged).** Signed grant **fully removed**; control-plane emits no capability. Pods are **Keycloak resource servers** + **pod-side OpenFGA** authz per request (fail-closed under `c3`). "Option Kept": managed instances + per-team tuning **retained**, resolved via a new **team-scoped ReBAC endpoint** (`GET /teams/{id}/agent-instances/{id}/runtime`) — **fixes F2**. Per-agent Keycloak audience (code+doc). Grant removed across fred-sdk/fred-core/control-plane/runtime/config/JSON-schemas/chart; frontend clients regenerated (team_id now in `runtime_context`). Lib minors bumped (core 3.4.0/sdk 3.3.0/runtime 3.3.0). All offline suites green (core 203/sdk 186/runtime 379/cp 174/agents 33/frontend tsc 0). **Not in branch (deployment):** NetworkPolicies + e2e TLS, realm audience mappers, per-pod client_id Helm values. PR #1857 (signed grant) NOT to be merged. **Review hardening rev. 2.1 (2026-06-28, Codex review, 7 findings all valid & fixed):** F-A OpenAI-compat off+OpenFGA-gated+c3 fail-closed; F-B identity from JWT (body tokens neutralized); F-C private-per-owner sessions; F-D no-team→403 + agent_id direct forbidden in c3; F-E strict exact iss/aud; F-F profile desc + schemas. F-G (3 pods chart + NetworkPolicies) = infra, separate. Suites green (core 207/sdk 186/runtime 386/cp 174/agents 33). **Pre-PR convergence rev. 2.2 (2026-06-28):** knowledge-flow now applies `apply_security_profile` (C3 enforced by control-plane + fred-agents + knowledge-flow); chart surfaces `security.profile` on the 3 request authorities + `deploy/README.md` classification-tiers section; self-test team-id canonicalized (`personal-<uid>`); frontend uses generated `ExecutionPreparation`/`RuntimeExecuteRequest`; **docs converged to rev. 2** (RUNTIME-EXECUTION-CONTRACT §0–§3/§8.11, CONTROL-PLANE-PRODUCT-CONTRACT, ARCHITECTURAL-SECURITY-REPORT rewritten, AGENT_POD_RUNTIME_PROTOCOL §2.3); `fred-samples` verified non-breaking. | [BACKLOG §3c.2.2](backlog/BACKLOG.md) | [EXECUTION-GRANT-SECURITY-HARDENING-RFC](rfc/EXECUTION-GRANT-SECURITY-HARDENING-RFC.md) | GitHub issue #1853 (PR #1857 superseded) |
| `RUNTIME-09` | `RUNTIME-GLOBAL-PROMPT-INJECTION` | Simon | Implemented - global base prompt (Mermaid contract) moved from authoring-time bake to runtime suffix injection; new agents only (no migration). Offline suites green (sdk/runtime/agents). Awaiting review. | [FRED-RUNTIME-QUALITY §RUNTIME-09](backlog/FRED-RUNTIME-QUALITY.md) | [SDK-V2-RFC §18.4 (revised)](rfc/SDK-V2-RFC.md) | GitHub issue #1867 |
| `CHAT-15` | `CHAT-UI-PHASE6` | Simon | Implemented 2026-06-29 (awaiting review) - attachments default-on when the search-documents tool is active. `chat_options.attach_files` declared next to `documents_selection` on `mcp-knowledge-flow-mcp-text` (default true) + `mcp-knowledge-flow-corpus` (default false) in `mcp_catalog.yaml`, and on `mcp-text` in Helm `values.yaml`. Resolver reads it purely from active-server `config_fields` (no legacy seed — no deployed instances); generic `McpServerCard` loop renders it (special-case + dead `serverCarriesChatOptions` removed); agent-level `FieldSpec` dropped from `general_assistant`/`react_rag_mcp`. No OpenAPI regen. Offline suites green (cp 175 / agents 33 / frontend 276). | [CHAT-UI-BACKLOG §16](backlog/CHAT-UI-BACKLOG.md) | [MCP-CATALOG-CONFIG-FIELDS-RFC §10](rfc/MCP-CATALOG-CONFIG-FIELDS-RFC.md) | GitHub issue #1870 |
| `FILES-01` | `AGENT-FILESYSTEM` | Dimitri | In progress - consolidated documentation: 4-root UX, simple SDK, agent outputs in Agents. | [CHAT-UI-BACKLOG §4.5](backlog/CHAT-UI-BACKLOG.md) | [FILESYSTEM](design/FILESYSTEM.md) | `TBD` |
| `FILES-02` | `MINDMAP-AGENT` | Marc | Not started - code reservation. | `TBD` | [FILESYSTEM](design/FILESYSTEM.md) | `TBD` |
| `FILES-03` | `COMPARISON-AGENT` | Dimitri | Delivered - `fred.dt.comparison.graph` agent, code-quality and tests green. | [BACKLOG §Phase AGENTS](backlog/BACKLOG.md) | [SIMILARITY-COMPARISON-AGENT-RFC](rfc/SIMILARITY-COMPARISON-AGENT-RFC.md) | branch `1772-...-kf-similarity-search` |
| `FILES-04` | `UNIFIED-FILESYSTEM` | Dimitri | In progress - **v1 feature-complete (G1-G7) on 2026-06-24**, ready for end-to-end testing: agent routing + runtime isolation (G1), provenance + badges (G4), 4-root UI + Agents root, human share-by-copy (G5), label deduplication (G6), SDK `read_user`/`read_team` helpers + `resolve_template` fix (G7). Remaining items are deliberate deferrals only: G1b (KF-side principal hardening, trusted agents) and G5/read_user refinements. | [CHAT-UI-BACKLOG §4.6](backlog/CHAT-UI-BACKLOG.md) | [FILESYSTEM](design/FILESYSTEM.md) | branch `...-final` |
| `FILES-05` | `BIG-FILE-TRANSFER` | Dimitri | Backlog - `/fs` buffers the whole file in memory (no streaming), OOM-prone for large files. Re-opens the streaming proxy vs presigned MinIO decision for large files only; needs a short RFC. | [CHAT-UI-BACKLOG §4.7](backlog/CHAT-UI-BACKLOG.md) | [AGENT-FILESYSTEM-HARDENING-RFC](rfc/AGENT-FILESYSTEM-HARDENING-RFC.md) | `TBD` |
| `FILES-06` | `GCS-STORAGE-BACKEND` | Dimitri | In progress - native GCS backend delivered; internal V4 signed URLs (IAM signBlob, no key) implemented for Parquet tabular reads, with startup fail-fast validation and URL masking in errors. Remaining gate: live GKE validation (RFC §1bis). | [BACKLOG §Phase STORAGE](backlog/BACKLOG.md) | [GCS-TABULAR-SIGNED-URL-RFC](rfc/GCS-TABULAR-SIGNED-URL-RFC.md) | branch `1795-...-native-google-cloud-storage-backend-signed`; GitHub issue #1795 |
| `CHAT-04` | `CHAT-ATTACHMENTS-OPTION-A` | Simon | Not started - dedicated branch required. | [CHAT-UI-BACKLOG §4](backlog/CHAT-UI-BACKLOG.md) | Option A: composer upload UX, base64 image context, drag-and-drop ingestion; MCP filesystem hardening tracked separately in FILES-01 | GitHub issue #1706 |
| `VALID-01` | `VALIDATION-E2E` | Simon | Blocked. | [BACKLOG §3b.7](backlog/BACKLOG.md) | - | `TBD` |
| `VALID-02` | `ADMIN-SELF-TEST` | Dimitri | **Phase 1 delivered** - rebuilt as UI-driven over the real pipeline (no backend module): reusable frontend pipeline engine + deterministic test agent `fred.github.self_test` (auto-enrolled/deleted, hidden from catalog via `AGENT-VISIBILITY-RFC`). Found and fixed a global RAG bug (`embeddinggemma` index vs `vector-index-mistral`). Prompt paths delivered (deterministic, no LLM): system prompt (tuning) + marketplace prompt (personal prompt -> session -> `context_prompt_text`), with the agent returning the received marker. **Second platform bug found and fixed**: runtime lost `context_prompt_text` while rebuilding `RuntimeContext` (`agent_app.py`), so marketplace prompts never reached the agent (regression test added). Remaining Phase 2 = real chat components + search modes; Phase 3 = GKE CronJob around every 2h. | [BACKLOG §3b.7](backlog/BACKLOG.md) | [ADMIN-SELF-TEST-HARNESS-RFC](rfc/ADMIN-SELF-TEST-HARNESS-RFC.md) | GitHub issue #1828 |
| `VALID-03` | `SELF-TEST-CORPUS` | Dimitri | Delivered (Phase 1) - golden corpus (two libraries, unique fact marker, assertions on cited document) written inline in the frontend, created in the admin personal space and deleted on each run. Phase 2: terms that distinguish keyword vs semantic search. | [BACKLOG §3b.7](backlog/BACKLOG.md) | [ADMIN-SELF-TEST-HARNESS-RFC §4](rfc/ADMIN-SELF-TEST-HARNESS-RFC.md) | GitHub issue #1828 |
| `CHAT-03` | `CHAT-OPTIONS` | Marc | In progress. | [CHAT-UI-BACKLOG §3](backlog/CHAT-UI-BACKLOG.md) | - | GitHub issue `#1730` |
| `CTRLP-12` | `FRED-2.0.2-RGPD-READY` | Dimitri | ✅ **Done — shipped in v2.0.2 (#1914).** Provable conversation erasure: `erase_session` fans out over every store (transcript, runtime checkpoint, attachments + KF embeddings) and returns an auditable `ErasureReceipt`; KPI anonymised, not deleted; metadata row deleted last so partial failures stay retryable. Team-governed retention on `team_metadata` (`GET/PATCH /teams/{id}`, platform-capped, default immediate-delete) where the retention window doubles as the evaluation window. Server-initiated deferred delete converges via a Temporal-reconciled purge queue (queue row done only on `receipt.ok`; flags `stalled` after N attempts, never auto-fails). Erasure schedule for platform & team admins. Bundled `DOC-RENAME` + `DOC-TAGS`. **Deferred follow-ups (next up):** member-removal task emission; real-conversation eval + cancel; eval authz (EVAL-01). | [BACKLOG §6.4.H](backlog/BACKLOG.md) | [FRED-2.0.2-RGPD-READY-RFC](rfc/FRED-2.0.2-RGPD-READY-RFC.md) | PR #1914 |
| `CTRLP-13` | `FRED-2.0.2-RGPD-READY` | Dimitri | **In progress** — RGPD lifecycle completion (parent `CTRLP-12`). **Shipped:** shared `TaskActivity` UI (platform + team), erasure out of team Settings. **Next (backend):** `remove_team_member` emits `schedule_erasure_task(reason=member_removed)`; `IDLE_EXPIRED` sweep + `last_activity_at` writer; per-row reason; contract §11/line-465 refresh. No schema change. | [BACKLOG §6.4.I](backlog/BACKLOG.md) | [RFC §6](rfc/FRED-2.0.2-RGPD-READY-RFC.md) | branch `fixes` (UI) |
| `CHAT-11` | `CHAT-VOICE-DICTATION` | Dimitri | In progress. | [CHAT-UI-BACKLOG §12](backlog/CHAT-UI-BACKLOG.md) | [CHAT-VOICE-DICTATION-RFC](rfc/CHAT-VOICE-DICTATION-RFC.md) | Waived GitHub issue for local Codex session |
| `MEMORY-02` | `MEMORY-CHECKPOINT-ISOLATION` | Marc | In progress. | [MEMORY BACKLOG §F.1](backlog/MULTI-AGENT-MEMORY-BACKLOG.md) | [MULTI-AGENT-MEMORY-HARDENING-RFC](rfc/MULTI-AGENT-MEMORY-HARDENING-RFC.md) | `TBD` |
| `MEMORY-03` | `MEMORY-REMOTE-AGENT` | Dimitri | In progress. | [MEMORY BACKLOG §F.2](backlog/MULTI-AGENT-MEMORY-BACKLOG.md) | [MULTI-AGENT-MEMORY-HARDENING-RFC](rfc/MULTI-AGENT-MEMORY-HARDENING-RFC.md) | `TBD` |
| `MEMORY-04` | `MEMORY-LOCAL-AGENT` | Dimitri | In progress. | [MEMORY BACKLOG §F.3](backlog/MULTI-AGENT-MEMORY-BACKLOG.md) | [MULTI-AGENT-MEMORY-HARDENING-RFC](rfc/MULTI-AGENT-MEMORY-HARDENING-RFC.md) | `TBD` |
| `MEMORY-05` | `MEMORY-HISTORY-CAP` | Simon | Best effort. | [MEMORY BACKLOG §F.4](backlog/MULTI-AGENT-MEMORY-BACKLOG.md) | [MULTI-AGENT-MEMORY-HARDENING-RFC](rfc/MULTI-AGENT-MEMORY-HARDENING-RFC.md) | `TBD` |
| `PROMPT-04` | `PROMPT-AGENT-FORM` | Dimitri | Planned after `MEMORY-REMOTE-AGENT` + `MEMORY-LOCAL-AGENT`; completion scope consolidated in prompt hardening RFC. | [BACKLOG §3d.9](backlog/BACKLOG.md) | [PROMPT-SYSTEM-HARDENING-RFC](rfc/PROMPT-SYSTEM-HARDENING-RFC.md) | `TBD` |
| `EVAL-01` | `EVAL-HARNESS` | Marc | Best effort. | [AGENT-EVALUATION-BACKLOG](backlog/AGENT-EVALUATION-BACKLOG.md) | [AGENT-EVALUATION-RFC](rfc/AGENT-EVALUATION-RFC.md) | `TBD` |
| `EVAL-02` | `EVAL-TASK-EVENTS` | Dimitri | **Folded into OPS-04 rev. 2 (2026-07-07)** - the multi-source tray/Activity spec now lives in OPS-04 §3.4; the amendment RFC is a tombstone. Execution (evaluator mounts `/evaluation/v1/tasks*`, drops bespoke campaign SSE) still tracked in the evaluation backlog. | [AGENT-EVALUATION-BACKLOG §EVAL-02](backlog/AGENT-EVALUATION-BACKLOG.md) | [OPS-04 §3.4/§5](rfc/TASK-EVENT-STREAM-RFC.md) (was AGENT-EVALUATION-TASK-EVENT-AMENDMENT-RFC) | branch `odelia` |
| `EVAL-03` | `EVAL-AUTH` | Odélia | **In progress** - Solution A: `service_agent` recognized for execution scoped to `team_id` (read-only `can_read`, no OpenFGA tuple) at **three** enforcement points — fred-runtime + control-plane (branch `1890`) **and now knowledge-flow** team-scoped corpus read (PR #1923), so RAG evaluations retrieve the team corpus instead of an empty result; tests green (fred-core/runtime/cp/knowledge-flow). Reusable **service-account pattern for async agent workers** (RFC EVAL-AUTH §11) — evaluator is first consumer. Deploy invariant: grant `service_agent` only to trusted M2M clients. Worker M2M identity switch pending in fred-agent-evaluator. A-vs-C-static agreed in meeting. | [AGENT-EVALUATION-BACKLOG §EVAL-03](backlog/AGENT-EVALUATION-BACKLOG.md) | EVAL-AUTH-RFC (fred-agent-evaluator/docs/rfc) | PR #1923 / branch `1890` |
| `TEAM-01` | `TEAM-CONFIG-RFC` | Dimitri | In progress (RFC-only). | [BACKLOG §3d.11](backlog/BACKLOG.md) | [TEAM RFC set](rfc/FRED-TEAM-CONFIG-RFC.md) | `TBD` |
| `CTRLP-10` | `CTRLP-10` | Dimitri | ✅ **Done 2026-07-15** - personal isolation + runtime hardening + 6.4.F delivered. Residual fixed 2026-07-09: managed-chat leaked the bare `"personal"` alias into fred-runtime `runtime_context.team_id`, causing a 403 on first message (GitHub issue #1959). Final residual fixed 2026-07-15: the nav-link placeholder — `router.tsx`'s static index redirect never resolved to `personal-{uid}`, so the URL stayed on the alias and `TeamSelectionNavbar` lost its selected highlight once bootstrap resolved. | [BACKLOG §6.4.F](backlog/BACKLOG.md) | [PERSONAL-TEAM-ISOLATION-RFC](rfc/PERSONAL-TEAM-ISOLATION-RFC.md) | GitHub issue #1959 / branch `1666-ctrlp-10-per-user-personal-space-replace-shared-team_id-constant` |
| `CTRLP-09` | `RUNTIME-DYNAMIC-ROUTING` | Simon | Not started - RFC written. | [BACKLOG §3d.12](backlog/BACKLOG.md) | [DISCOVERED-RUNTIME-ROUTING-RFC](rfc/DISCOVERED-RUNTIME-ROUTING-RFC.md) | `TBD` |
| `PROMPT-05` | `PROMPT-CONTEXT-PICKER` | Dimitri | Closed 2026-06-19 - multi-prompt (0..N ordered) wired: `session_context_prompts` table, `context_prompt_ids`, control-plane concatenation, pills + scope-grouped picker. | [BACKLOG §3d.9](backlog/BACKLOG.md) | [PROMPTS](design/PROMPTS.md) | branch `1779-fully-wire-prompts-in-the-chat-ui-page` |
| `PROMPT-08` | `PROMPT-RUNTIME-INJECTION` | Dimitri | Closed 2026-07-06 (#1915) - selected chat-context prompts never reached the model: `context_prompt_text` hit the agent binding but no runtime appended it to the system prompt. New shared `compose_system_prompt` + `build_context_prompt_suffix` for ReAct **and** Deep (also fixes a latent Deep attachment-suffix gap); control-plane resolution now scoped to caller's active + personal teams. Composer + runtime `build_executor` + cross-team regression tests. | [BACKLOG §3d.9](backlog/BACKLOG.md) | [PROMPTS](design/PROMPTS.md) §5/§7 | GitHub issue #1915 / branch `1915-chat-context-prompts-never-reach-the-model` |
| `CTRLP-04` | `AGENT-MODEL-PROFILES` | Dimitri | Waiting. | [BACKLOG §3d](backlog/BACKLOG.md) | - | `TBD` |
| `RUNTIME-05` | `REACT-THOUGHT-SURFACE` | Dimitri | In progress - Layer 1 + 2b + 2c delivered (Mistral reasoning -> THOUGHT_*, JSON leakage fix + multi-turn HTTP 422 replay fix); Layer 2 / Rico demo deferred. | [FRED-RUNTIME-QUALITY §RUNTIME-05](backlog/FRED-RUNTIME-QUALITY.md) | [AGENT-THINKING-API-RFC §Amendment A](rfc/AGENT-THINKING-API-RFC.md) | GitHub issue `#1757` / branch `1757-featruntime-05-support-mistral-reasoning-chunks-in-thought-stream` |
| `PROMPT-06` | `PROMPT-MARKETPLACE` | Dimitri | Waiting for `PROMPT-04`; marketplace remains copy-by-value forward work. | [BACKLOG §3d.10](backlog/BACKLOG.md) | [PROMPT-SYSTEM-HARDENING-RFC](rfc/PROMPT-SYSTEM-HARDENING-RFC.md) | `TBD` |
| `FRONT-09` | `KNOWLEDGE-WORKSPACE-REWORK` | Dimitri | In progress - A/C/D delivered 2026-06-18: `TeamResourcesPage` at `/team/:teamId/resources` (tree + paginated list + CRUD, documents only). Remaining: B (backend browse hardening), E (detail drawer), rename (no endpoint). | [FRONTEND-BACKLOG §15](backlog/FRONTEND-BACKLOG.md) | [KNOWLEDGE-WORKSPACE-REWORK-RFC](rfc/KNOWLEDGE-WORKSPACE-REWORK-RFC.md) | branch `1772-...-kf-similarity-search` |
| `FRONT-13` | `DOCUMENT-VIEWER-AI-PANEL` | TBD | RFC proposed - unify the chat-citation (`/documents/:uid`) and corpus-workspace preview flows into one `DocumentViewer` component; restore native PDF rendering (code exists, currently unwired — GitHub issue #1956); add an assistant side panel reusing the existing `selected_document_uids` chat-scoping mechanism. | [FRONTEND-BACKLOG §19](backlog/FRONTEND-BACKLOG.md) | [DOCUMENT-VIEWER-AI-PANEL-RFC](rfc/DOCUMENT-VIEWER-AI-PANEL-RFC.md) | `TBD` |
| `UX-01` | `UX-AUDIT` | Dimitri | Not started. | [BACKLOG §UX-1](backlog/BACKLOG.md) | [COMPONENT-UX](ux/COMPONENT-UX.md) | `TBD` |
| `FRONT-05` | `FRONTEND-CLEANUP` | Dimitri | In progress - rework 20->15 (swap identical `VectorSearchHit`/`LinkPart` types to runtime, 2026-06-26). Remaining: `ChatMessage`/`Channel`/`AwaitingHumanEvent` (frontend); `FinishReason` = decision between frontend union and runtime OpenAPI enum (possible backend change, RFC first). | [FRONTEND-BACKLOG §7](backlog/FRONTEND-BACKLOG.md) | - | GitHub issue [#1840](https://github.com/ThalesGroup/fred/issues/1840) |
| `FRONT-08` | `FRONTEND-AUTH-CONFIG` | Simon | In progress - implemented on branch, awaiting review. | [FRONTEND-BACKLOG §14](backlog/FRONTEND-BACKLOG.md) | [FRONTEND-AUTH-CONFIG-ENDPOINT-RFC](rfc/FRONTEND-AUTH-CONFIG-ENDPOINT-RFC.md) | GitHub issue `#1748` / branch `1748-front-08-frontend-auth-config` |
| `FRONT-10` | `FRONTEND-AUTH-CONFIG` | Dimitri | Closed - fix GCU chicken-and-egg: `gcu_version` served on the public pre-auth `/frontend/config` surface (bootstrap still returns 403 before acceptance); effective value (null when gating off); fixes missing GCU screen + "Control plane unavailable". | [FRONTEND-BACKLOG §16](backlog/FRONTEND-BACKLOG.md) | [FRONTEND-AUTH-CONFIG-ENDPOINT-RFC §7](rfc/FRONTEND-AUTH-CONFIG-ENDPOINT-RFC.md) | branch `1793-unified-virtual-filesystem` |
| `MIGR-00` | `KEA-SWIFT-CUTOVER` | Florian | Migration model: 4 topics (identity -> data -> metadata -> products) + cherry-picks/parity. | [KEA-MIGRATION-BACKLOG](backlog/KEA-MIGRATION-BACKLOG.md) | [KEA_SWIFT_CUTOVER](ops/KEA_SWIFT_CUTOVER.md) | `TBD` |
| `MIGR-04` | `KEYCLOAK-IDENTITY-BOOTSTRAP` | Sébastien | Platform prerequisite - preserve `sub` (UUID) on S3NS before any data import. | [KEA-MIGRATION-BACKLOG §0](backlog/KEA-MIGRATION-BACKLOG.md) | [KEYCLOAK-IDENTITY-BOOTSTRAP-S3NS](ops/KEYCLOAK-IDENTITY-BOOTSTRAP-S3NS.md) | `TBD` |
| `MIGR-06` | `MIGRATION-DATA-MIRROR` | Dimitri | **Data** topic - `mc mirror` MinIO key-for-key (laptop bridge, around 25 GB). | [KEA-MIGRATION-BACKLOG §0ter](backlog/KEA-MIGRATION-BACKLOG.md) | - | `TBD` |
| `MIGR-05` | `PLATFORM-IMPORT-SERVICE` | Dimitri | **Metadata** topic - In progress (9/12). Shipped + live-tested: atomic import (agents+tags+metadata, BackgroundTask not Temporal), swift-native export, reset, Platform-data stats. Next gap: agent prompt transfer (05.11); deferred: OpenFGA restore (05.04, ops Option A), stage reconciliation (05.07). | [KEA-MIGRATION-BACKLOG §0bis](backlog/KEA-MIGRATION-BACKLOG.md) | [PLATFORM-IMPORT-RFC](rfc/PLATFORM-IMPORT-RFC.md) | branch `swift` |
| `MIGR-07` | `MIGRATION-PRODUCTS-REVECTORIZE` | Dimitri | **Products** topic - re-vectorization on target (Temporal on `output_process`, RFC written). | [KEA-MIGRATION-BACKLOG §0quater](backlog/KEA-MIGRATION-BACKLOG.md) | [CORPUS-REVECTORIZE-RFC](rfc/CORPUS-REVECTORIZE-RFC.md) | `TBD` |
| `DEVOPS-FREDLAB` | `DEVOPS-FREDLAB` | Sébastien | **CRITICAL - remaining work is the Helm chart for internal GCP / GKE Autopilot.** | [BACKLOG §3b](backlog/BACKLOG.md) | - | `TBD` |
| `OPS-01` | `DEVOPS-HELM-CHART` | Simon | Not started - CI + Docker prerequisite closed. | [BACKLOG §3b.11](backlog/BACKLOG.md) | [FRED-CHART-MODERNIZATION-RFC](rfc/FRED-CHART-MODERNIZATION-RFC.md) | GitHub issue `#1685` |
| `OPS-04` | `TASK-EVENT-STREAM` | Dimitri | In progress — core confirmed. **rev. 2 (proposed):** single home for the worker-action **audit log** + **one shared Activity surface**; documents shipped `erasure` kind + `scheduled_for`, adds `deletion` kind + total-coverage invariant + audit retention/immutability + **central table** (control-plane owns; producers via `RemoteTaskClient` → single-source frontend). Folds in **EVAL-02** (→ tombstone) + **CTRLP-13** display half. **Shipped:** shared `TaskActivity` UI; **P4 (2026-07-12)** ingestion live-feedback wiring (TaskTray mounted, upload drawer closes on schedule not full completion, new-document refetch trigger) found missing via live testing. **Next:** central-table backend consolidation (deferred past finalization). | [BACKLOG §OPS-04](backlog/BACKLOG.md) | [TASK-EVENT-STREAM-RFC](rfc/TASK-EVENT-STREAM-RFC.md) | branch `fixes` (UI); branch `1912-authz-05-...` (P4) |
| `DEVOPS-FREDLAB` | `DEVOPS-FREDLAB` | Sébastien | **CRITICAL - Helm chart closed, launch internal GKE Autopilot deployment.** | [BACKLOG §3b](backlog/BACKLOG.md) | - | `TBD` |
| `OPS-05` | `DEVOPS-STORAGE-NAMING` | Simon | Not started - RFC/backlog ready. | [BACKLOG §OPS-05](backlog/BACKLOG.md) | [OBJECT-STORAGE-NAMING-RFC](rfc/OBJECT-STORAGE-NAMING-RFC.md) | `TBD` |
| `OPS-06` | `DEPENDABOT-CLEANUP` | Dimitri | In progress — 185/208 alerts were phantom (deleted manifest paths), dismissed. Of the 23 real alerts, 22 fixed same day (dompurify, torch/torchvision, sentence-transformers, pytest, langchain patch); code-quality + full tests green, real-model smoke test passed. 1 blocked: `transformers` can't clear its "high" alert until `docling-ibm-models` supports `huggingface_hub>=2`. | [BACKLOG §OPS-06](backlog/BACKLOG.md) | - | GitHub issue `#1938` |
| `LICENSE-01` | `COPYLEFT-AUDIT` | Dimitri | **High severity, decision made 2026-07-09, not yet implemented, milestone `swift-golive`.** `pymupdf`/`pymupdf4llm` (AGPL-3.0/Artifex) are today a mandatory dependency (every default build/image contains them). Decision: move to an optional dependency group with `docling`/`pypdf` as the real default everywhere, so the default Fred distribution ships with no AGPL code. **Full plugin separation deferred to `INGEST-01`** (draft, not started) — target no earlier than the `swift ga` milestone (2026-09-30); confirming status with Timothé. | [COPYLEFT-DEPENDENCIES §LICENSE-01](COPYLEFT-DEPENDENCIES.md#license-01--pymupdf--pymupdf4llm-agpl-30) | - (RFC not required for Option 1; full separation would ride on `INGEST-01`) | GitHub issue [#1950](https://github.com/ThalesGroup/fred/issues/1950) |
| `LICENSE-03` | `COPYLEFT-AUDIT` | Dimitri | **Option A done 2026-07-09** — `jwcrypto` (transitive via `python-keycloak`'s `KeycloakAdmin`) disclosed in README + id-legend; low severity, unmodified/dynamically-imported, never directly invoked by Fred's own JWT path (PyJWT). **Option B (eliminate `python-keycloak`, direct `httpx` calls to the Admin REST API) opened as a separate follow-up, not implemented.** | [COPYLEFT-DEPENDENCIES §LICENSE-03](COPYLEFT-DEPENDENCIES.md#license-03--jwcrypto-lgpl-30-or-later) | - (mechanical dependency substitution, no RFC) | GitHub issue [#1949](https://github.com/ThalesGroup/fred/issues/1949) |
| `OBSERV-02` | `KPI-ANALYTICS-PERSONAL-DASHBOARD` | Dimitri | **Implemented 2026-07-12 (awaiting review, no GitHub issue yet).** Page 3 only (personal token-usage dashboard: timeline + breakdown by agent + by model). 3 new self-scoped presets (`user_token_usage_over_time`/`_by_agent`/`_by_model`) reusing the already-emitted `agent.turn_completed` event; fixed a latent bug found along the way — `quantities.input_tokens`/`output_tokens` were written but never mapped in the OpenSearch index, so unaggregatable. New `TeamUsagePage` (`team/:teamId/usage`) reuses `AnalyticsPage`'s chart primitives; entry point is a new icon on the personal-space banner. `make code-quality` + `make test` green in `control-plane-backend`, `fred-core`, `frontend`. Live-stack validation still pending. | [BACKLOG §Phase 7b](backlog/BACKLOG.md) | [KPI-ANALYTICS-RFC §2.5](rfc/KPI-ANALYTICS-RFC.md) | `TBD` |

## Execution Refs Already Known

| Ticket     | Owner     | Status | Backlog                                             | Execution                                                                                                    | PMO note                                                                                                                                                                                                                                                                                           |
| ---------- | --------- | ------ | --------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `FRONT-07` | Dimitri   | Closed | [FRONTEND-BACKLOG §13](backlog/FRONTEND-BACKLOG.md) | GitHub issue `#1668` / branch `1668-rework-frontend-ui-architecture-compliance`                              | Extracted `SearchField`, `FilterChips`, and `TagInput` molecules; migrated `PromptsPage` + `TuningFieldRenderer`; added `--outline-variant` token; removed dead CSS.                                                                                                                              |
| `CHAT-04`  | Simon     | Closed | [CHAT-UI-BACKLOG §4](backlog/CHAT-UI-BACKLOG.md)    | GitHub issue `#1706` / branch `1706-chat-04-chat-attachments-option-a-composer-upload-ux-scheduler-task-ui` | CHAT-04 complete for Swift: composer UX Option A + drag-and-drop, control-plane `session_attachments` persistence (`summary_md` + `storage_key`), side file drawer with markdown preview, reload rehydration, and strong deletion orchestrated through Knowledge Flow. Validation: control-plane, knowledge-flow, and frontend `make code-quality` + `make test`. |
| `OPS-01`   | Simon     | Closed | [BACKLOG §3b.11](backlog/BACKLOG.md)                | GitHub issue `#1685`                                                                                         | Modern Helm chart aligned on `fred-agents`; `runtime_catalog_sources`, `/fred/agents/v2`, `k3d` overlays, frontend proxy, and probes aligned; validation `helm template` + `make code-quality` + `make test`.                                                                                     |
| `CHAT-10`  | Marc      | Closed | [CHAT-UI-BACKLOG §11](backlog/CHAT-UI-BACKLOG.md)   | Branch `feature/swift-test`                                                                                  | `MarkdownRenderer` now recognizes `mindmap` / `mindmap-json` fences and routes them to `MindMapBlock`, with JSON validation, node-count guardrail, interactive tree, and raw fallback on parsing error.                                                                                            |
| `OPS-03`   | Simon     | Closed | [BACKLOG §3b.13](backlog/BACKLOG.md)                | GitHub issue `#1664`                                                                                         | Modern Docker packaging aligned on `fred-agents`; images and startup contracts are considered closed for the Swift target GKE Autopilot stack.                                                                                                                                                      |
| `OPS-02`   | Sébastien | Closed | [BACKLOG §3b.12](backlog/BACKLOG.md)                | GitHub issue `#1663`                                                                                         | Swift CI aligned on modern artifacts: build/push `fred-agents`, `control-plane-backend`, `knowledge-flow-backend`, and `frontend`, with chart validation still tied to `deploy/charts/fred`.                                                                                                      |
| `CHAT-09`  | Dimitri   | Closed | [CHAT-UI-BACKLOG §10](backlog/CHAT-UI-BACKLOG.md)   | GitHub issue `#1654`                                                                                         | Hardened streaming block-fence UX: single CodeBlock shell during streaming for Mermaid, code, math, and directives; final specialized rendering when the fence closes. Live pod validation remains non-blocking.                                                                                     |
| `CTRLP-06` | Florian   | Open   | [BACKLOG §3.10](backlog/BACKLOG.md)                 | GitHub issue `kea #1601`                                                                                     | Partial fix already done; error aggregation and structured 422 body remain.                                                                                                                                                                                                                        |
| `CHAT-12`  | Dimitri   | Closed | [CHAT-UI-BACKLOG §13](backlog/CHAT-UI-BACKLOG.md)   | Branch `1772-featknowledge-flow-targeted-similarity-comparison-search-kf-similarity-search`                  | Extracted shared `MenuPopover` + `MenuPopoverItem` molecules (profile menu grammar); `UserProfile` and `SearchConfig` now use them. `SearchConfig` lost its frames and uppercase labels: "Attach files" became an item, Document/Search/Scope are homogeneous items with inline value + chevron. "Prompts" row (PROMPT-05) is ready but not wired (blocked by PROMPT-03). Frontend `make code-quality` + `make test`. |
| `CHAT-14`  | Dimitri   | Closed | [CHAT-UI-BACKLOG §15](backlog/CHAT-UI-BACKLOG.md)   | GitHub PR `#1816` (merged) - external contribution @pdubey28-sketch                                         | Readable tool-call labels in the trace + privacy hardening: `humanizeToolName` turns raw MCP identifiers into action labels; the trace no longer exposes tool names, arguments, or result payloads (action + status + latency only); `tool_call` dedup by `call_id`. Tests `traceUtils.test.ts`. (PR auto-labelled CHAT-12 by mistake; recorded as CHAT-14, CHAT-13 reserved for #1819.) Follow-up: remove dead `summarizeToolResultCompact` / `toolResultContent` code. |

`TEAM RFC set` currently means:

- `FRED-TEAM-CONFIG-RFC.md`
- `TEAM-PLATFORM-POLICY-RFC.md`
- `TEAM-ROUTING-POLICY-RFC.md`

Prompt-library scope is no longer a separate historical RFC in this set; the
current behavior is in [PROMPTS](design/PROMPTS.md), with follow-up hardening in
[PROMPT-SYSTEM-HARDENING-RFC](rfc/PROMPT-SYSTEM-HARDENING-RFC.md).
